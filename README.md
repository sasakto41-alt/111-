# Majestic Text Helper v3.2.1

Хранилище игровых фраз для Majestic RP и быстрая вставка в чат.
**Enter никогда не нажимается программой** — подтверждение чата всегда за вами.

## Что нового в v3.2.1

1. **Часовой пояс госволны** — в Настройках появился выбор пояса
   («Как на компьютере» или UTC−12…UTC+14). Влияет на «Подобрать время»
   и проверку правил в разделе «Госволна» — удобно, если сервер живёт
   по другому времени. На странице «Госволна» видно, какой пояс сейчас
   используется и сколько на нём времени.
2. **Диагностика запуска** — если программа не смогла открыться, отчёт
   сохраняется в `data/error.log` и показывается окно с ошибкой.
   Пришлите содержимое файла — и причину можно будет исправить.
3. **run.bat** — запуск из исходников без сборки exe (сам ставит зависимости).

## Что нового в v3.2.0

1. **Способ вставки «Копировать в буфер»** — самый надёжный. Программа просто
   копирует текст в буфер обмена и переключает вас в окно игры. Вы сами
   нажимаете `T` и вставляете `Ctrl+V`. Игра уже не может «съесть» ввод.
2. **Раздел «Госволна»** — помощник подачи государственных волн по памятке:
   - кнопка «Подобрать время» предлагает ближайшие допустимые слоты
     (за 10–120 минут, сетка 10 минут, интервал ≥ 20 минут);
   - автосборка команд: узнать занятость → занять → «Занял гос.волну» →
     `/report` → освободить;
   - шаблоны `/gnews` для **Палето-Бэй** и **Сенди-Шорс** (редактируемые);
   - кнопка «Сохранить команды в библиотеку» — категории «Госволна»
     с возможностью назначить горячие клавиши;
   - ссылка на Discord-канал объявления.
3. **Окно игры (цель переключения)** — в настройках укажите часть заголовка
   окна игры (например `Majestic`) — программа сама найдёт окно, переключится
   на него перед нажатием `T` или после копирования.
4. **Права администратора** — exe теперь сам запрашивает запуск от
   администратора (`uac_admin`). Это лечит случай, когда игра запущена от
   администратора и «глушит» хоткеи и ввод обычных программ.
   При запуске Windows один раз спросит разрешение — это нормально.

## Быстрый старт

### Вариант 1: собрать exe самостоятельно
1. Установите Python 3.10+ (галочка *Add Python to PATH*).
2. Запустите `build_exe.bat`.
3. Готовый файл: `dist/MajesticTextHelper.exe` (portable, данные в `data/`).

### Вариант 2: запуск из исходников
```
pip install -r requirements.txt
python main.py
```
или просто запустите `run.bat` — он сам установит зависимости и запустит программу.

## Первый запуск — рекомендации

1. **Запускайте от администратора** (exe сам попросит).
2. Откройте **Настройки → Окно игры → «Захватить активное окно (10 с)»**,
   кликнув по окну игры за 10 секунд — поля заполнятся сами. Нажмите
   «Проверить» — должно найтись минимум 1 окно.
3. Если вставка в игре не срабатывает — выберите
   **Способ вставки: «Копировать в буфер»**. Тогда в игре: `T` → `Ctrl+V` → `Enter`.
4. Хоткей меню по умолчанию — `F6`. Меню поверх игры: выберите фразы,
   клик по карточке — вставка/копирование, `Esc` — скрыть.

## Способы вставки (Настройки)

| Способ | Как работает |
|---|---|
| Печатать посимвольно (Unicode) | Сама открывает чат (`T`), ждёт, печатает текст |
| Вставить через Ctrl+V | Сама открывает чат (`T`), копирует, жмёт `Ctrl+V` |
| **Копировать в буфер** | Только копирует и переключает в игру — `T` и `Ctrl+V` нажимаете сами |

## Горячие клавиши

- `F6` (настраивается) — показать/скрыть меню поверх игры.
- Для каждой фразы можно назначить свою клавишу (`F1–F24`, `ctrl+1` и т.п.).
- Фразы из категории «Госволна» удобно вешать на `F7`…`F9`.

## Госволна — памятка (встроена в программу)

- Занимать волну: **за 10–120 минут** до вещания.
- Слоты: **XX:00 / XX:10 / XX:20 / XX:30 / XX:40 / XX:50**.
- Между объявлениями вашей организации: **≥ 20 минут**; общих: **≥ 10 минут**.
- Не более **3 волн в час**. Повторное занятие — после публикации объявления.
- Порядок: проверить занятость → занять (если тишина 5 минут) →
  «Занял гос.волну» → объявление в Discord → `/report` → освободить после публикации.

## Данные

- Всё хранится в `data/data.json` рядом с программой (portable).
- Если файл повреждён — программа делает бэкап `*.corrupt-*.json` и стартует заново.
- Тест без ввода: `set MTH_DRYRUN=1` перед запуском — нажатия только логируются.

## Сборка

`build_exe.bat` ставит зависимости и собирает onefile-exe через
`majestic_helper.spec` (без консоли, иконка, `uac_admin=True`).

## FAQ

**Программа (exe) не открывается.** Проверьте по порядку:
1. Windows SmartScreen: нажмите **«Подробнее» → «Выполнить в любом случае»**
   (файл без цифровой подписи — это нормально).
2. Windows спрашивает права администратора — нажмите **«Да»**.
   Если отменить — программа закроется и не откроется.
3. Антивирус может ругаться на сборку (keyboard + PyInstaller) — добавьте
   папку программы в исключения.
4. Если появилось окно с ошибкой или есть файл `data/error.log` —
   пришлите его содержимое, там точная причина.
5. Нет Python? Для запуска без сборки используйте `run.bat`.

**F6 в игре не открывает меню.** Запустите программу от администратора
(сборка v3.2.0 делает это автоматически). Если игра в полноэкранном
эксклюзивном режиме — переключите её в окренный/безрамочный режим.

**Текст печатается не туда.** Укажите «Окно игры» в настройках — программа
сама переключится на нужное окно перед вставкой.

**Безопасный режим:** способ «Копировать в буфер» — программа не нажимает
вообще никаких клавиш, только кладёт текст в буфер обмена.
